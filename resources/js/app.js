import './bootstrap';
import 'bootstrap/dist/js/bootstrap.bundle.min.js';
import AOS from 'aos';

document.addEventListener('DOMContentLoaded', () => {
    AOS.init({
        duration: 900
    });
});